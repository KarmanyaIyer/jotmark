{
  "name": "jotmark",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for keeping timestamped notes per website or per page, stored locally.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.mjs",
    "qa": "node scripts/qa.mjs",
    "icons": "node scripts/render-icons.mjs",
    "package": "node scripts/package.mjs"
  },
  "license": "MIT",
  "author": "Karmanya Iyer",
  "devDependencies": {
    "puppeteer-core": "^25.7.0"
  }
}
